<Window x:Class="ADODesigner.Views.EditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ADODesigner.Views"
        mc:Ignorable="d"
        Title="ADODesigner - Editor" WindowStartupLocation="CenterScreen" Height="700" Width="1200" Background="#FF131517" WindowStyle="None">
    <Window.DataContext>
        <local:EditorVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Menu Background="Transparent" VerticalAlignment="Center">
            <MenuItem Header="File">
                <MenuItem Header="Save..." InputGestureText="Ctrl+S"/>
                <MenuItem Header="Convert to .adofai..." InputGestureText="Ctrl+D"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Copy" InputGestureText="Ctrl+C"/>
                <MenuItem Header="Paste" InputGestureText="Ctrl+V"/>
                <MenuItem Header="Cut" InputGestureText="Ctrl+X"/>
            </MenuItem>
            <MenuItem Header="Animations">
                <MenuItem Header="Balls Animation..."/>
            </MenuItem>
        </Menu>
<<<<<<< Updated upstream
        <GridSplitter Width="5" Grid.Row="1" Background="#FF101113"/>
=======
        <Border Background="#FF101013" Grid.Row="1" Grid.Column="0" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Border Background="#FF121215">
                    <TextBlock Text="Properties" Margin="10,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource TextColor2}"/>
                </Border>
            </Grid>
        </Border>
        <Border Background="#FF101013" Grid.Row="1" Grid.Column="1" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Border Background="#FF121215">
                    <TextBlock Text="Editor" Margin="10,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource TextColor2}"/>
                </Border>
                <Canvas x:Name="PART_Editor" Grid.Row="1">
                    
                </Canvas>
            </Grid>
        </Border>
        <GridSplitter Width="5" Grid.Row="1" Background="Transparent"/>
>>>>>>> Stashed changes
    </Grid>
</Window>
