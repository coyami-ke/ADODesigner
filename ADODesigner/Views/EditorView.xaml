<Window x:Class="ADODesigner.Views.EditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ADODesigner.ViewModels"
        mc:Ignorable="d"
        Title="EditorView" Height="640" Width="1000" Background="{DynamicResource ForegroundColor}" WindowStyle="None">
    <Window.DataContext>
        <vm:EditorVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.6*"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="0.6*"/>
        </Grid.ColumnDefinitions>
        <Border Background="{DynamicResource BackgroundColor}" Grid.Row="1">
            <ListBox Style="{DynamicResource ListBoxStyle}" ItemsSource="{Binding Decorations}">

            </ListBox>
        </Border>
        <Border Grid.Column="1" Grid.Row="1" Background="#FF101013">
            <TabControl Background="Transparent" BorderBrush="#FF101013" BorderThickness="5">
                <TabItem Style="{DynamicResource TabItemStyle}" Header="Designer">
                    <ScrollViewer Style="{DynamicResource ScrollViewerStyle}" HorizontalScrollBarVisibility="Visible" Background="#FF121215">
                        <Grid Height="500" Width="500">

                            <Rectangle Fill="White" Height="100" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20 0 0 540"/>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
                <TabItem Style="{DynamicResource TabItemStyle}" Header="TimeLine">
                    <ScrollViewer Style="{DynamicResource ScrollViewerStyle}" HorizontalScrollBarVisibility="Visible" Background="#FF121215">
                        
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</Window>
